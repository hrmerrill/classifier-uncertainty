site_name: classifier-uncertainty
theme:
  name: material
plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          paths: [src]
          options:
            docstring_style: numpy
nav:
  - Home: index.md
  - API Reference: api.md
